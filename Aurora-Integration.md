# Aurora Integration — Vera‑CDE WorkPanel

Il WorkPanel comunica con Aurora tramite:

- endpoint RPC
- smart contract CDC Registry

Funzioni previste:
- registerContract()
- signContract()
- anchorEvidence()
- getContractState()
